#####################################################
# This is example .env file
# To get the project working properly you need to 
# populate the environmental variables
# Read more https://www.npmjs.com/package/dotenv
#####################################################
DB_USERNAME = 'isisuserdb'
DB_PASSWORD = 'pa55w0rdTolocalDB'
DB_CONNECTION_STRING = '(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=bisapps-db)(PORT=1521))(CONNECT_DATA=(SERVER=DEDICATED)(SERVICE_NAME=xepdb)(FAILOVER_MODE=(TYPE=SESSION)(METHOD=BASIC))))'
PORT = '8100'
BASE_URL = 'http://user-office-web-service:8080/users-service'
API_KEY = '' // use an api key which has permissions to add fap members to a fap for eaxmple panel-member-manager's api key
