query getProposalsWithCallInfo($filter: ProposalsFilter) {
  proposals(filter: $filter) {
    proposals {
      primaryKey
      proposalId
      title
      call {
        shortCode
        title
        id
      }
    }
  }
}