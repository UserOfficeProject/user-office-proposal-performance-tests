mutation AssignFapReviewersToProposals(
  $assignments: [FapReviewAssignmentInput!]!
  $fapId: Int!
) {
  assignFapReviewersToProposals(
    assignments: $assignments
    fapId: $fapId
  ) {
    id
  }
}